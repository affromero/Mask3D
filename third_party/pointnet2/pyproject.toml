[build-system]
requires = ["setuptools", "torch"]

[tool.poetry]
name = "pointnet2"
packages = [{include = "pointnet2"}]
version="0.0.1"
authors = ["Jonas Schult"]
description = "https://github.com/JonasSchult/Mask3D version of Pointnet2: https://github.com/erikwijmans/Pointnet2_PyTorch"